# Review-only illustration of the adoption API fields. The current runtime does
# not execute adoption or target/type replacement. Do not apply this as a
# migration command; keep writes stopped and consult the main README.
# Never create a claim or write status.phase=Confirmed by hand.
# An existing claim must not be reused for a new record identity.
metadata:
  annotations:
    fortigate-external-dns.kgskr.io/adopt: "true"
spec:
  adoptionRequested: true
